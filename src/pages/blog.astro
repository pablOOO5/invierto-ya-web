---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import BlogImage from '../components/BlogImage.astro';
import { getAllPosts } from '../data/blogPosts';

// Obtener todos los posts usando la función centralizada
const posts = getAllPosts();
---

<BaseLayout 
  title="Blog de Inversiones - Invierto Ya"
  description="Descubre estrategias de inversión, análisis de mercado y consejos financieros en nuestro blog. Educación financiera práctica para inversores de todos los niveles."
>
  <main class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
    <!-- Header -->
    <header class="bg-white shadow-lg">
      <nav class="container mx-auto px-6 py-4 flex justify-between items-center">
        <div class="text-2xl font-bold text-blue-600">
          <a href="/">Invierto Ya</a>
        </div>
        <div class="hidden md:flex space-x-6">
          <a href="/" class="text-gray-700 hover:text-blue-600 transition-colors">Inicio</a>
          <a href="/blog" class="text-blue-600 font-semibold">Blog</a>
          <a href="/sobre-mi" class="text-gray-700 hover:text-blue-600 transition-colors">Sobre Mí</a>
          <a href="/contacto" class="text-gray-700 hover:text-blue-600 transition-colors">Contacto</a>
        </div>
      </nav>
    </header>

    <!-- Hero Section -->
    <section class="py-20">
      <div class="container mx-auto px-6">
        <div class="max-w-4xl mx-auto text-center">
          <h1 class="text-5xl font-bold text-gray-800 mb-6">
            📰 Blog de Inversiones
          </h1>
          <p class="text-xl text-gray-600 mb-8">
            Estrategias, análisis y consejos para construir tu futuro financiero
          </p>
          
          <!-- Categorías -->
          <div class="flex flex-wrap justify-center gap-4">
            <span class="bg-blue-100 text-blue-800 px-4 py-2 rounded-full text-sm font-medium">Todos</span>
            <span class="bg-gray-100 text-gray-700 px-4 py-2 rounded-full text-sm font-medium hover:bg-gray-200 cursor-pointer">Principiantes</span>
            <span class="bg-gray-100 text-gray-700 px-4 py-2 rounded-full text-sm font-medium hover:bg-gray-200 cursor-pointer">Estrategias</span>
            <span class="bg-gray-100 text-gray-700 px-4 py-2 rounded-full text-sm font-medium hover:bg-gray-200 cursor-pointer">Educación</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Posts Grid -->
    <section class="py-16 bg-white">
      <div class="container mx-auto px-6">
        <div class="max-w-6xl mx-auto">
          <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {posts.map((post) => (
              <article class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow">
                <BlogImage 
                  src={post.image}
                  alt={post.title}
                  class="w-full h-48 object-cover"
                />
                <div class="p-6">
                  <div class="flex items-center justify-between mb-3">
                    <span class="bg-blue-100 text-blue-800 text-xs font-medium px-2.5 py-0.5 rounded-full">
                      {post.category}
                    </span>
                    <time class="text-sm text-gray-500">{post.date}</time>
                  </div>
                  <h2 class="text-xl font-bold text-gray-800 mb-3 line-clamp-2">
                    {post.title}
                  </h2>
                  <p class="text-gray-600 mb-4 line-clamp-3">
                    {post.excerpt}
                  </p>
                  <a 
                    href={`/blog/${post.slug}`}
                    class="inline-flex items-center text-blue-600 font-medium hover:text-blue-800 transition-colors"
                  >
                    Leer más
                    <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                  </a>
                </div>
              </article>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Newsletter CTA -->
    <section class="py-16 bg-gradient-to-r from-blue-600 to-blue-700">
      <div class="container mx-auto px-6 text-center">
        <h2 class="text-3xl font-bold text-white mb-4">
          ¿No quieres perderte ningún artículo?
        </h2>
        <p class="text-xl text-blue-100 mb-8">
          Suscríbete y recibe nuestros mejores consejos de inversión directo en tu email.
        </p>
        <a href="/#newsletter" class="bg-white text-blue-600 font-bold py-4 px-8 rounded-lg hover:bg-gray-100 transition-colors inline-block">
          Suscribirme al Newsletter
        </a>
      </div>
    </section>
  </main>
</BaseLayout>
